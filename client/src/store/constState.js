import { atom } from "recoil";

export const constPlaylistState = atom({
  key: "constPlaylistState",
  default: [],
});

export const MusiclistState = atom({
  key: "MusiclistState",
  default: [],
});
